- content_for :page_title do
  = t('settings.strikes')

%p= t('disputes.strikes.description_html', instance: Rails.configuration.x.local_domain)

= render partial: 'shared/account_warning', collection: @strikes
